# Dry-Run Sample Inputs

These fixtures seed the Phase 4 automation dry-run rehearsals.

- `files_sample.json` lists the manifest entries for the rehearsal run (three updated files, including guardrail helpers, and one skipped helper).
- `tests_sample.json` captures the pytest invocations recorded in the rehearsal metrics summary (command center marker, library integration suite, and helper adoption regression).

Update these samples whenever the rehearsal scenario expands; keep them in sync with the generated bundle at `.repo_studios/command_center/reports/repo-studios__command-center__automation_run/`.
