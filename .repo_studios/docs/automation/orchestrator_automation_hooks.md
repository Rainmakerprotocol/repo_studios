# Orchestrator Automation Hooks

<!-- markdownlint-disable MD013 -->

This reference captures the current run surfaces, automation wiring, and success criteria for the Repo Studios
orchestrator tier. Record changes here before altering orchestrator code so downstream automation stays aligned
with expected behaviors.

| Script | Invocation Surface | Make Target | CI Hook | Expected Success Criteria |
| --- | --- | --- | --- | --- |
| `orchestrators/run_batch_cleanup.py` | `python .repo_studios/scripts/orchestrators/run_batch_cleanup.py [--mode ...] [--target ...] [--output-base ...] [--artifacts-to-keep ...] [--log-level ...] [--refresh-only]` | Pending (`studio-batch-clean` placeholder) | None | Command exits 0, creates `.repo_studios/reports/orchestrator_runs/run_batch_cleanup/run_batch_cleanup-<ts>/` bundles (`cleanup_summary.json`, `cleanup_log.txt`, `bundle_summary.json`), updates `latest_*` pointers, and records per-command status (including markdownlint availability). |
| `command_center/scripts/orchestrators/run_fault_diagnostics_overview.py` | `python .repo_studios/command_center/scripts/orchestrators/run_fault_diagnostics_overview.py [--repo-root ...] [--runs-dir ...] [--run-dir ...] [--reuse-report ...] [--producer-top-frames ...] [--skip-producer] [--skip-consumer] [--skip-summarizer] [--producer-artifacts-to-keep ...] [--consumer-artifacts-to-keep ...] [--summarizer-artifacts-to-keep ...] [--artifacts-to-keep ...] [--timestamp ...] [--log-level ...]` | `studio-orchestrate-fault-diagnostics` (alias `studio-run-fault-pipeline`) | None | Command exits 0, emits `.repo_studios/command_center/reports/commandview/fault_diagnostics/<slug>/` bundles (`manifest.json`, `summary.md`, `telemetry.json`), mirrors producer/consumer/summarizer pointers under their Command Center mirrors, enforces per-step retention, and captures pipeline telemetry for all stages. |
| `command_center/scripts/orchestrators/run_docs_health_overview.py` | `python .repo_studios/command_center/scripts/orchestrators/run_docs_health_overview.py [--repo-root ...] [--doc-index-output-dir ...] [--anchor-inventory-output-dir ...] [--anchor-validation-output-dir ...] [--docs-integrity-output-dir ...] [--metrics-stub-output-dir ...] [--churn-output-dir ...] [--undocumented-output-dir ...] [--placeholder-output-dir ...] [--monkey-patch-output-dir ...] [--aggregator-output-dir ...] [--healthview-root ...] [--artifacts-to-keep ...] [--doc-index-artifacts-to-keep ...] [--anchor-inventory-artifacts-to-keep ...] [--anchor-validation-artifacts-to-keep ...] [--docs-integrity-artifacts-to-keep ...] [--metrics-stub-artifacts-to-keep ...] [--churn-artifacts-to-keep ...] [--undocumented-artifacts-to-keep ...] [--aggregator-artifacts-to-keep ...] [--skip-* ...] [--timestamp ...] [--log-level ...]` | `studio-orchestrate-docs-health` | None | Command exits 0, refreshes Docs Health producer artifacts (unless skipped), blends signals through the aggregator, emits `.repo_studios/command_center/reports/healthview/docs_health/<slug>/` bundles (`manifest.json`, `summary.md`, `telemetry.json`), records the overall score/step telemetry payload, and honors retention budgets for each dependency plus the Healthview manifest directory. |
| `orchestrators/run_pytest_log_capture.py` | `python .repo_studios/scripts/orchestrators/run_pytest_log_capture.py [--repo-root ...] [--logs-dir ...] [--output-dir ...] [--artifacts-to-keep ...] [--log-level ...] [--from-log ...] [--from-junit ...] -- [pytest args...]` | None (invoked by health suite) | Planned addition to health suite once refactor lands | Writes `.repo_studios/reports/orchestrator_runs/pytest_log_capture/pytest_log_capture-<ts>/` bundles (`report.json`, `report.md`, `bundle_summary.json`, `failures.tsv/csv`, `skips.tsv/csv`, `full_log.txt`, optional `junit.xml`), refreshes `latest_*` pointers, stages raw logs under `.repo_studios/reports/orchestrator_logs/pytest_log_capture_logs/`, prunes history to `--artifacts-to-keep` (default five), and returns pytest's exit code (including serial retry fallback on xdist hangs). |
| `orchestrators/orchestrate_health_suite.py` | `python .repo_studios/scripts/orchestrators/orchestrate_health_suite.py [--timestamp ...] [--live]` | Pending (`studio-health-suite` placeholder) | None | Generates `.repo_studios/reports/orchestrator_logs/health_suite_logs/<ts>/` with `status.json` and `status.md` (mirrored into the legacy folder for consumers), preserves per-step logs, and records non-zero step exits in status payload while overall command exits 0. |
| `command_center/scripts/orchestrators/run_standards_integrity.py` | `python .repo_studios/command_center/scripts/orchestrators/run_standards_integrity.py [--repo-root ...] [--diff-old-index ...] [--prompt-formats ...] [--artifacts-to-keep ...] [--log-level ...] [--timestamp ...]` | `studio-orchestrate-standards` | Repo Studios Inventory Health (`.github/workflows/studio-inventory.yml`) | Command exits 0, emits `.repo_studios/command_center/reports/healthview/standards_integrity/<slug>/` bundles (`manifest.json`, `summary.md`, `telemetry.json`), mirrors catalog registration, applies per-step retention budgets, and captures pipeline telemetry matching step outcomes. |
| `orchestrators/run_standards_index_cli.py` | Automation: `python .repo_studios/scripts/orchestrators/run_standards_index_cli.py [--repo-root ...] [--output-dir ...] [--index-path ...] [--artifacts-to-keep ...] [--log-level ...] <subcommand>`<br>Interactive: `RUN_STANDARDS_INDEX_CLI_USE_LEGACY=1 python ...` (`PowerShell: $env:RUN_STANDARDS_INDEX_CLI_USE_LEGACY = '1'; ...`) | None (manual helper) | None | Subcommands (`list`, `search`, `show`, `stats`) exit 0, emit structured bundles under `.repo_studios/reports/orchestrator_runs/standards_index_cli/` (`report.json`, `report.md`, `bundle_summary.json`, `stdout.txt`), refresh `latest_*` pointers, and preserve stdout output for interactive use; non-zero exits record errors in the summary payload. Automation omits the legacy flag so the Standards Integrity redirect executes before the CLI response; operators set the flag only when they need to skip the redirect for ad-hoc queries. |

## Notes

- The command center Make target (`make -C .repo_studios command-center COMMAND_CENTER_TARGET=<path>`) now sits alongside
  `studio-orchestrate-fault-diagnostics` and `studio-orchestrate-docs-health`; remaining orchestrators still require manual invocation until their dedicated Make targets are added.
- Update this table whenever Make targets or CI hooks change so the script inventory can reference the latest wiring
  before orchestrator refactors proceed.

<!-- markdownlint-enable MD013 -->
